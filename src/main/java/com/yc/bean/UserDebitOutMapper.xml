<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yc.bean.UserDebitOutMapper">
	<!--加入带日志的ehcache缓存 -->
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"></cache>


	<select id="findUserDebitInIdByUserDebitInTypeId" resultType="int" parameterType="java.util.Map">
		select udi_id from UserDebitIn,UserDebitInType where udi_type = udit_id and udit_id = #{udit_id}
	</select>

	<insert id="addUplanUserDebitOut" parameterType="java.util.Map">
		insert into
		UserDebitOut(udi_id,u_id,udo_startdate,udo_date,udo_money)
		values( #{udi_id} , #{u_id} , #{udo_startdate} , #{udo_date},#{udo_money})
	</insert>

</mapper>	